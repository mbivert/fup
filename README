HTTP service allowing to deposit and withdraw temporary files.

Total capacity available for the uploader: 2Gio (Maxstorage)
Maximum size of a file : 5Mio (Maxsize)
Maximum duration of a file : 24h (Maxduration)

A goroutine check outdated files every 2 hours (Cleantime).

Flag "-port" changes default port (:8080).

Flag "-data" changes default data directory (./data).

To disable file listing in /data:

  % touch data/index.html

See http://fup.awesom.eu for a running example.

Public domain.
